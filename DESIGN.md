# 吃啥 · 落地方案设计（基于第一版UI）

> 依据：《第一版UI原型》`prototype_v1.html` 与 SKILL `eatwhat-v1-ui`。
> 本设计为后续开发蓝图：任何不一致处，以 SKILL（第一版交互基准）为准。

## 1. 目标与范围
- 解决「今天不知道吃啥」：给出菜谱（自己做）与餐厅（外卖/堂食）的组合推荐与执行管理。
- 一餐可同时含菜谱与餐厅（既点外卖又做几道菜）。
- 多端：Web(H5) 优先，页面/交互可复用到小程序与 App。

## 2. 总体架构
- 前端：单页应用（底部 5 Tab 恒定），页面与交互对齐第一版原型。
- 后端：Python（FastAPI）轻量 REST；数据库 SQLite（后续可平滑迁移）。
- 派生数据全部实时计算，不落冗余存储。
- 配置统一走「配置表」(键值)，环境变量集中管理；部署/上线流程固化为脚本。

## 3. 界面与导航（不变）
五个底部 Tab：推荐 / 菜谱（页顶标题「吃这些」）/ 冰箱 / 餐厅 / 我的。
- **推荐**：转盘「帮我选一个」（菜谱+餐厅混合池，带类型徽章，+候选 与 再抽一次 并排）+ 快捷口味筛选（横滑，选用餐人自动勾其口味，多选取并集）+ 「为您推荐」（1餐厅+3菜谱，「换一个」随机轮换）。
- **菜谱/吃这些**：候选收件箱 + 「按口味筛」横滑标签 + 全部菜谱（新建入口在标题旁）。
- **冰箱**：在库+待采购（行内双行，可增/改/删）、食材库、大类维护。
- **餐厅**：收藏列表 + 新增/编辑共用表单（含到达耗时/交通工具）。
- **我的**：统计 / 干饭记录 / 体重 / 参考菜谱 / 干饭成员 / 厨房技巧 / 设置。

## 4. 数据模型
- `users`，`configs(key,value)`（含审核开关、临期阈值等环境参数）。
- `recipes(id, source[my|admin], name, em 图标, time, diff, tags[], ing[], steps[])`：`source=admin` 即参考菜谱（只读）。
- `fridge_items(id, name, cat, qty, store, buy, days)`：在库；`purchase(name, qty)`：待采购。
- `eat_inbox(id, kind[recipe|shop], ref_id, name, em)`：候选；`tracks(id, inbox_id, started, paused, elapsed)`：多道并行计时。
- `shops(id, name, type, price, star, must[], note, arrive[], 到达耗时/交通)`。
- `diners(id, name, tags[])`：干饭成员/用餐人，口味道味各自维护。
- `tips(id, title, content, cat, author, pub, status[pending|approved|rejected])`：厨房技巧（人人可看可增，只改自己，增/改须审核）。
- `records(id, date, name, type[cook|out|delivery])`：饮食记录（唯一 id，可编辑/删除）。
- `weights(id, date, weight, body_fat)`。

## 5. 关键交互实现要点（映射第一版）
1. **候选**：`+候选`入 `eat_inbox`，停留原页；已选按钮不可重复。
2. **待采购代入/回退（COOK 一致性）**：加入菜谱候选时，逐食材比对在库，缺则 `purchase` 数量累加；从候选移除时按量回退。餐厅不参与。同一事务内完成，保证一致。
3. **计时**：吃这些行内直接 开始/停止/取消；多道并行；进详情按状态切换（计时中→走表控件；未计时→「开始做这道菜」）。
4. **口味筛选**：chips 单行横滑；点用餐人自动带入其口味（并集），可手动取消；据此过滤推荐。
5. **参考菜谱**：管理员录入、只读；可「加入吃这些」或「存进我的菜谱」。我的菜谱可新建/编辑。
6. **审核闭环**：厨房技巧新增/编辑 → `pending`；管理员通过→`approved`/驳回→`rejected`；仅自己的或已公开可见。
7. **统计/体重**：派生（如体脂曲线）实时计算。

## 6. 权限摘要
- 看：全部（推荐/菜谱/餐厅/冰箱/我的）。
- 厨艺技巧：人人可看可增；只改自己；新增与编辑管理员审核；公开可全体见。
- 我的菜谱仅自己编辑；参考菜谱只读。

## 7. 上线流程（固化）
配置表驱动（审核开关、临期阈值等）→ 一键初始化数据库 → 单元/接口测试 → 前端联调 → 按南方脚本部署。

## 8. 迭代节奏（建议）
1. 后端骨架 + Schema + 一线接口（菜谱/食材/候选/待采购/记录）。
2. 前端骨架承接 5 Tab，先把「吃这些+待采购」跑通。
3. 用餐人口味联动、计时、审核闭环。
4. 体重/统计、设置与上线固化。